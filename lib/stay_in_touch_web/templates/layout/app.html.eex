<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title><%= assigns[:page_title] || "StayInTouch · Gerencie os contatos daquela turma inesquecível." %></title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <%= csrf_meta_tag() %>
  </head>
  <body>
    <header>
      <section class="container">
        <h1>Stay in Touch</h1>
        <nav role="navigation">
          <%= if signed_in?(@conn) do %>
            <ul>
              <li><%= link "Meu perfil", to: Routes.user_path(@conn, :edit, "2") %></li>
              <li><%= link "Sair", to: Routes.session_path(@conn, :delete) %></li>
            </ul>
          <% else %>
            <ul>
              <li><%= link "Cadastre-se", to: Routes.user_path(@conn, :new) %></li>
              <li><%= link "Entrar", to: Routes.session_path(@conn, :new) %></li>
            </ul>
          <% end %>
        </nav>
      </section>
    </header>
    <main role="main" class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= render @view_module, @view_template, assigns %>
    </main>
    <script type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
